# Ignore environment files
.env
.env.local

# Ignore sensitive frontend config (if ever added for dev)
config.js
config/config.js

# Ignore node modules (if you have backend or local tools)
node_modules/

# Ignore build output (if built locally before deploy)
dist/
build/
.next/

# Optional: OS / editor files
.DS_Store
*.log
.vscode/
